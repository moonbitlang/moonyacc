%start start

%token A
%token B

%type<String> start
%type<String> x
%type<String> y

%%

start
  : x { $1 }
  ;

%inline x
  : A { "xa" }
  | A y B { "xb" }
  ;

%inline y
  : A A { "ya" }
  | B B { "yb" }
  ;
