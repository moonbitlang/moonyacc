import {
  "moonbitlang/core/list",
}

warnings = "-6"

formatter(ignore: [ "inline.mbt" ])

rule(
  name: "moonyacc",
  command: "moon run --target wasm $mod_dir/.. -- $input -o $output",
)

dev_build(rule: "moonyacc", input: "inline.mbty", output: "inline.mbt")
