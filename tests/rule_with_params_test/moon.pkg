import {
  "moonbitlang/core/list",
}

warnings = "-6"

rule(
  name: "moonyacc",
  command: "moon runwasm $mod_dir/.. -- $input | moonfmt > $output",
)

dev_build(rule: "moonyacc", input: "parser.mbty", output: "parser.mbt")
