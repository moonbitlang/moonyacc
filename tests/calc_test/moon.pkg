import {
  "moonbitlang/core/list",
}

rule(
  name: "moonyacc",
  command: "moon runwasm $mod_dir/.. -- --input-mode pull $input | moonfmt > $output",
)

dev_build(rule: "moonyacc", input: "calc.mbty", output: "calc.mbt")
