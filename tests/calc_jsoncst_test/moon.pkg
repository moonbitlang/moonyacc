import {
  "moonbitlang/core/list",
}

rule(
  name: "moonyacc",
  command: "moon runwasm $mod_dir/.. -- --mode json-cst --input-mode pull --force-token-no-payload $input | moonfmt > $output",
)

dev_build(
  rule: "moonyacc",
  input: "calc_jsoncst.mbty",
  output: "calc_jsoncst.mbt",
)
