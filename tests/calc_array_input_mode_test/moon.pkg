import {
  "moonbitlang/core/list",
}

rule(
  name: "moonyacc",
  command: "moon runwasm $mod_dir/.. -- $input --input-mode array | moonfmt > $output",
)

dev_build(rule: "moonyacc", input: "calc.mbty", output: "calc.mbt")
